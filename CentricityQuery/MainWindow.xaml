<Window x:Class="CentricityQuery.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CentricityQuery"
        mc:Ignorable="d"
        FontSize="14"
        Title="MainWindow" Height="750" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Expander Header="Query Details" IsExpanded="True">
            <Grid>
                <Grid.Resources>
                    <Style TargetType="RowDefinition">
                        <Setter Property="Height" Value="35"/>
                    </Style>
                </Grid.Resources>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <ComboBox Grid.Row="0" Width="200" 
                          ItemsSource="Tables"
                          VerticalAlignment="Center" Margin="2" Padding="3" FontSize="14" HorizontalAlignment="Left">
                   
                </ComboBox>
            </Grid>
        </Expander>
        <Label Content="Blah" Grid.Row="1"/>
    </Grid>
</Window>
